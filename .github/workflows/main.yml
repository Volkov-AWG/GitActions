on:
    push:
        branches:
            - master

jobs:
    build:
        name: Hello action
        runs-on: ubuntu-latest
        steps:
            - name: Clone repository
              uses: actions/checkout@v1s
              
            - name: Stary yml
              uses: ./action-a
              with: 
                MY_NAME: "Aleksandr"
